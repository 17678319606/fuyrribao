# 副业日报自动化管线 — 全量审计报告（v2.10）

> 审计时间：2026-08-14
> 审计范围：`generate_report.py`（AI 生成）、`publish_wp.py`（WordPress 发布）、`daily-report.yml`（GitHub Actions 流水线）
> 审计目标：确保管线在**网络抖动 / 断流 / 源站超时 / 网关偶发异常**场景下仍能稳定完成每日日报更新，不会"静默失败"。

---

## 一、历史故障复盘（修复前的真实翻车记录）

此前已通过真实跑测复现并修复了 5 类故障（run 编号对应 GitHub Actions）：

| Run | 故障现象 | 根因 | 修复版本 |
|-----|---------|------|---------|
| 31728298083 | 步骤2 崩溃 | 流式返回空 `choices` chunk，`obj.get("choices",[{}])[0]` 越界 | v2.4 容错跳过空 choices |
| 31729438280 | 步骤2 崩溃 | `AI_PROXY_POOL` 填了中文占位符「代理IP:端口」被当真实代理 → `InvalidURL` | v2.5 过滤占位符代理 + 捕获 InvalidURL |
| 31729629965 | 步骤2 崩溃 | AI 返回 JSON 字符串值内含未转义 `}`，`rfind("}")` 截错导致 JSON 残缺 | v2.6 括号平衡栈提取 |
| 31729943358 | 步骤2 崩溃 | 流式被网关中途截断（内容在 `ToB` 处硬断），原逻辑解析失败直接 `SystemExit` 不重试 | v2.7 截断检测 + 重试 |
| 31730208471 | 成功但"未更新" | 脚本检测到当日文章已存在→跳过发布（防重复逻辑误伤手动验证） | v2.8 手动触发 `FORCE_UPDATE=1` 覆盖 |

> 结论：上述 5 类均为**程序健壮性缺陷**，已全部修复并经真实 run 回归验证。

---

## 二、本轮全量审计发现的新增薄弱点 + 修复

### 🔴 P0 — 工作流无自愈（核心风险）
- **问题**：原流水线 3 步串行，**任意一步失败即整跑失败**。若北京 18:00 整点恰遇 AI 网关/WordPress 瞬时抖动或短时不可用，当天日报**直接放弃，永不重试**（原无 job 级重试、无备用 cron）。
- **修复（v2.9 + v2.10）**：
  1. 步骤 1-3 合并为单步「**自愈重试循环**」：任一环节失败自动 `sleep 60` 后重试，最多 3 轮（`MAX_TRIES=3`），仅当 3 轮全失败才标记作业失败。
  2. 新增 **18:30 备用补发 cron**（`30 10 * * *`）：主跑（18:00）遇**持续中断**（>3 分钟）未能发布时，备用跑 30 分钟后兜底补发。因 `publish_wp.py` 对"已存在且完整"的文章自动跳过，**成功日不会重复发文**，仅失败日补发。
- **效果**：瞬时抖动（单跑内 3 轮重试）+ 持续中断（跨跑备用 cron）双保险。

### 🔴 P0 — 已发布残缺文章无法自愈
- **问题**：原 publish 逻辑"存在即跳过"。若某次因 AI 截断发布了**残缺/空文章**，后续 cron 永远认为"已存在"而跳过，**残缺文章永久滞留**。
- **修复（v2.8 + 本轮确认）**：`publish_wp.py` 改为 **upsert + broken 自愈**：当日文章若 HTML 缺失 `shr-card`（判定为残缺），即便非 `FORCE_UPDATE` 也强制覆盖修复。

### 🟡 P1 — git push 偶发 non-fast-forward
- **问题**：步骤4 直接 `git push`，曾因 Actions 并行/历史漂移导致 `non-fast-forward` 拒绝，状态回写失败。
- **修复（v2.9）**：推送前先 `git pull --rebase origin <branch>`，失败重试 3 次。

### 🟡 P1 — job 超时不足
- **问题**：原 `timeout-minutes: 15`，叠加 3 轮重试 + AI 长生成可能撑爆。
- **修复**：提升至 `timeout-minutes: 30`，容纳重试耗时。

### 🟢 P2 — 采集脚本无内部重试（已间接覆盖）
- `fetch_signals.py` 本身无重试，但已被纳入**工作流自愈循环**，瞬时失败由外层 3 轮兜底。

---

## 三、已就位的多层可靠性设计（防御纵深）

### AI 调用层（`generate_report.py`）
| 防御层 | 机制 | 参数 |
|--------|------|------|
| 端点轮换 | 直连优先 + 代理池随机打散 | `_candidate_endpoints()` |
| 每端点重试 | 指数退避（3/6/12/24s） | `RETRY_PER_ENDPOINT=4` |
| 生成级重试 | 空生成/解析失败/结构残缺重生成 | `GEN_RETRIES=3` |
| DNS 兜底 | DoH 多 provider（Google/Cloudflare/1.1.1.1）monkey-patch `socket.getaddrinfo` | `_doh_resolve` |
| 防 524 | 流式请求 `stream=True` | — |
| 防中文乱码 | 强制 `decode("utf-8")` | — |
| 空 choices 容错 | 流式 chunk `choices=[]` 直接跳过 | — |
| JSON 完整性 | 括号平衡栈提取 + trailing comma/注释兜底 | `_extract_json` |
| 截断自愈 | 解析失败=本次尝试失败→退避重试 | — |
| 非流式兜底 | 流式全失败改整包请求（规避分块截断） | — |
| 占位符代理过滤 | 过滤中文/字面占位符（如"代理IP:端口"） | `_is_valid_proxy` |
| 双超时 | connect 15s / read 180s | `REQ_TIMEOUT` |

### 发布层（`publish_wp.py`）
- upsert：当日文章存在则更新（`FORCE_UPDATE=1` 或 broken 自愈），否则新建。
- broken 自愈：残缺文章（无 `shr-card`）自动覆盖修复。
- 空日报保护：0 条且无方法论时不发布。

### 工作流层（`daily-report.yml`）
- 自愈重试循环（3 轮 × 60s 间隔）。
- 双 cron（18:00 主 + 18:30 备用补发）。
- rebase 安全推送（3 次重试）。
- 超时 30min。

---

## 四、实测验证（真实 run 回归）

| Run | 版本 | 结果 | 线上校验 |
|-----|------|------|---------|
| 31748427038 | v2.9 | ✅ success | 13454 更新为 21px 新排版（modified 06:06:47） |
| 31748668215 | v2.10 | ✅ success | 13454 保持 21px 新排版（modified 06:09:40） |

校验项（线上 API 实测）：
- `font-size: 21px` 卡片标题：✅ 存在
- `font-size: 19px` 旧排版：❌ 已消失
- `shr-card` 卡片容器：✅ 存在
- 乱码：✅ 无（UTF-8 强制解码已生效）

---

## 五、残余风险与建议

1. **密钥真实性**：`AI_SIDEHUSTLE_API_KEY` / `WP_APP_PASSWORD` 此前疑似填过占位符，现经多轮实测已可用（run 成功即证明）。若将来网关/WordPress 改密，需同步更新 Secrets。
2. **代理池未启用**：`AI_PROXY_POOL` 仍为占位符，当前走纯直连（已验证 GitHub 海外 Runner 直连 `ai.jinbufenzi.com` 稳定）。如需双保险，填入真实国内出口代理即可，**零代码改动**。
3. **超长持续中断（>30 分钟）**：18:30 备用跑仍失败则当天缺失。如需更强保障，可再加 19:00 第三备用 cron（同理安全）。
4. **AI 网关自身宕机**：属上游依赖，管线已用重试+备用 cron 最大化容错，但无法替代网关可用性。

---

## 六、结论

✅ 经过 5 轮真实故障修复 + 本轮全量审计加固，管线已形成**防御纵深**：脚本内多层重试（端点/生成/JSON/截断）+ 工作流自愈循环（3 轮）+ 跨跑备用 cron（18:30）+ 幂等自愈发布。**网络抖动、瞬时断流、源站超时均不会导致日报漏更**；仅上游网关长时间（>30min）宕机才会影响，属合理边界。
