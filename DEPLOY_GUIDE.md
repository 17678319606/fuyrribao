# 副业日报 · 全链路部署与配置指南（最终版）

## 架构一句话

CI（**CNB 主力** / GitHub 备用）只做「采集 → AI 生成 → 发布到 WordPress」；**公众号推送由 WordPress 插件 `fuyr-wechat-pusher` 在你的服务器源站直接调微信 API**（出口 IP 已在白名单），彻底免代理、免 tinyproxy。

```
CNB/GitHub 定时(06:00/19:00)
      │  采集→AI→发WP
      ▼
WordPress「日报」分类文章（同日覆盖更新，19:00 为最全版）
      │  save_post 钩子（北京≥19点 + 当日去重）
      ▼
fuyr-wechat-pusher 插件（源站 PHP 直连 api.weixin.qq.com）
      │  自动合成"底图+北京时间"封面 → 上传素材 → freepublish
      ▼
微信公众号发布
```

---

## 一、安装 WP 插件（二选一）

### 方式 A：WP 后台上传 zip（推荐，最简单）
1. 下载附件 `fuyr-wechat-pusher.zip`
2. WP 后台 → **插件 → 安装插件 → 上传插件** → 选该 zip → 安装并**启用**「副业日报 · 公众号推送」

### 方式 B：宝塔文件管理器
1. 进入 `/www/wwwroot/dajiayouxuan.com/wp-content/plugins/`，新建文件夹 `fuyr-wechat-pusher`
2. 把 `fuyr-wechat-pusher.php` 上传进去
3. WP 后台 → 插件 → 启用「副业日报 · 公众号推送」

---

## 二、配置插件（WP 后台 → 设置 → 公众号推送）

逐项填写（已附默认值）：

| 字段 | 填什么 | 说明 |
|------|--------|------|
| **AppID** | `wx83fcc867e12e7695` | 你公众号的 AppID |
| **AppSecret** | 你的 AppSecret | 之前在 secrets 里的那个；**现在只填这里，不再进 CI 密钥库** |
| **推送方式** | `freepublish` | 发布（不占群发额度，推荐）；想先试水选 `draft` 存草稿；认证号才用 `mass` |
| **作者名** | `副业日报` | 显示在公众号图文作者处 |
| **末次触发小时(北京)** | `19` | 北京 ≥19 点才推，保证是当日最全版 |
| **触发分类** | `日报` | 只有属于「日报」分类的文章才会推，其他文章绝不误推 |
| **封面图** | 留空即可 | 插件已**内置一张地图风格默认底图**（含「副业日报」标题，不含时间），不传也自动用它；每次推送时 GD 在底图上叠加北京日期/时间，上传微信后**用完即删、不落 WP 库**。想换风格可上传自定义底图覆盖。 |

保存即可。

---

## 三、验证连通性 + 确认白名单 IP（关键一步）

1. 设置页底部点「**立即推送最新日报**」按钮（忽略时间门槛，专为测试）
2. 看返回结果：
   - ✅ **成功**：页面顶部绿条「推送成功：已发布(publish_id)=…」，去公众号看文章
   - ⚠️ **`invalid ip X.X.X.X, not in whitelist`**：把里面的 `X.X.X.X` 加进**微信公众号 IP 白名单**（这个 IP 就是源站真实出口 IP，可能 ≠ 反代 `122.51.106.193`）
   - ⚠️ **`未设置封面底图`**：回去传一张封面底图
   - ⚠️ **GD 相关报错**：服务器没开 PHP-GD，联系主机开启；或插件会自动退化为直接用底图推送（只是没叠时间）

---

## 四、CNB 密钥库 env.yml（整段替换，只留敏感密钥）

**做法**：去 `jinbufenzi/fuyrribao-secrets` 仓库网页编辑 `env.yml`，**清空原有内容，整段粘贴下面这份**（AI 网关地址/模型、WP 站点/账号/分类已全部写进代码默认，这里只放敏感密钥；真实值见随附的 `env.fuyrribao.clean.yml`，**不要手抄错**）：

```yaml
# ===== AI 主用（璇玑国内网关）=====
# 网关地址与模型已写死在代码(generate_report.py)，这里只放敏感密钥
ai_api_key: "<你的璇玑API Key>"
# 兜底（仅璇玑不可用时；可选）
GEMINI_API_KEY: "<你的Gemini Key>"

# ===== WordPress =====
# 站点/账号/分类已在代码默认(dajiayouxuan.com / tougao / 日报)，只放应用密码
WP_APP_PASSWORD: "<你的WP应用密码>"

# ===== 公众号（推送交由 WP 插件在源站直连微信）=====
# AppID/AppSecret 只在 WP 后台填写，不进 CI 密钥库
WX_PUSH_VIA_WP: "1"

# ===== 企业微信通知 =====
WXWORK_WEBHOOK: "<你的企业微信机器人Webhook>"
```

> 真实密钥以随附的 `env.fuyrribao.clean.yml` 为准（那份含实际值，供你直接复制粘贴到 secrets 仓库；它不进 git 仓库，仅本地交付）。
> GitHub 那侧作为备用：workflow 已不再读 `WX_*`，且 AI 网关/模型、WP 站点/账号/分类也都改成走代码默认，只保留 `ai_api_key`/`GEMINI_API_KEY`/`WP_APP_PASSWORD`/`WXWORK_WEBHOOK` 四个 Secrets 即可。

---

## 五、跑通 CNB

- **定时**：每天北京 `06:00`（首版发 WP）、`19:00`（末版发 WP → 触发插件推公众号）
- **手动验证**：CNB 网页 `web_trigger` 触发一次，看 WP 是否出新文章、插件是否推了公众号

---

## 六、你 4 个问题的结论

1. **只传「日报」最终版、不误传？** 能。分类门槛（只推「日报」分类）+ 时间门槛（北京≥19）+ 当日去重（同日仅一次）+ CI 同日覆盖更新同一篇（19:00 是最全版），多重保险。
2. **封面自动换时间、WP 不存图？** 已实现。底图只存一张，插件用 GD 叠加北京时间生成临时图上传，用完即删。
3. **白名单放源站还是反代 IP？** 实测为准——插件是源站 PHP 直连微信，微信看到的是源站出口 IP。装好插件点「立即推送」，报错里的 `invalid ip X.X.X.X` 就是真出口 IP，加它进白名单即可。
4. **CNB 能实现吗？免费够用？** 能，且推荐做主力。CNB 已不调微信（微信由源站插件调），所以 CNB 动态 IP 对白名单无影响；每天仅 2 次轻量构建，免费额度绰绰有余，国内访问稳。

---

## ⚠️ 安全提醒（务必处理）

前面多轮对话里 **GitHub PAT、微信 AppSecret、企业微信 webhook key、璇玑 key、WP 密码** 都明文出现过，建议这套稳定跑通后，统一去各平台**重置/吊销**一遍，避免泄露风险。
